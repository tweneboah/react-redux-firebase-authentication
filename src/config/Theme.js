import { createMuiTheme } from "@material-ui/core";

const Theme = createMuiTheme({
  palette: {
    common: {
      blue: "#341f97"
    },
    primary: {
      main: "#222f3e"
    },
    secondary: {
      main: "#2e86de"
    }
  }
});

export default Theme;
